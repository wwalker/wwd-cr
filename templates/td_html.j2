            <td style="overflow:hidden;max-width:150px;max-height:150px;">
                <a href="{{url}}">
              <div class="button">
                  {%- if image == "" -%}
                    <font size="75">{{label}}</font>
                  {% else %}
                    <img src="/images/{{image}}" alt="{{label}}" width="{{image_width}}px" height="{{image_height}}px" />
                  {%- endif -%}
              </div>
                </a>
            </td>
